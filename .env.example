# Sunny Tides — environment variables
# Copy to .env.local and fill in. Without STRIPE_SECRET_KEY the storefront
# runs in demo mode (checkout completes without charging a card).

# Stripe secret key (test: sk_test_..., live: sk_live_...)
STRIPE_SECRET_KEY=
